"use strict";

const UTILS = require("./utils");

class EMSState {
	constructor(context, timeout = 500) {
		this.context = context;
		this.timeout = timeout;
	}
	getPerson() {
		// TODO: Handle getPerson
	}
	createPerson() {
		// TODO: Handle createPerson
	}
	createEvidence() {
		// TODO: Handle createEvidence
	}
}

module.exports = EMSState;
